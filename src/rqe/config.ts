
export const VerboseTraceFindMatch = false;
export const VerboseFunctionOutputFiltering = false;
export const BreakOnMatchFailure = false;

// CallPoint
export const VerboseLogFilteredEmptyValue = false;

// Matching
export const WarnOnMultipleMatches = false;

// MemoryTable
export const MemoryTableExtraErrorMessages = true;

// # Streams #
export const StreamOverflowCheck = true;
export const StreamOverflowCheckMs = 10000;

// Print every message that every stream receives.
export const StreamSuperTrace = false;

// Adds to StreamSuperTrace and also prints the stack trace every time a message happens.
export const StreamSuperDuperTrace = false;

// net
export const SocketClientCheckOutgoingData = true;

export const StreamValidation = true;

export const EnableWarningOnUnserializableData = true;

// Debugging console.log
export const StackTraceAllConsoleLogs = false;

export const EnableSerializableTest = true;

export const TaggedValueErrorOnUnknownTag = false;

export const SocketVerboseLog = false;

